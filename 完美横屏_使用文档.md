# 完美横屏 · 使用文档

> 模块名：完美横屏（`com.github.lsposed.magicwindow`）
> 简介：让更多应用支持平行窗口、横屏和大屏布局
> 类型：LSPosed / Xposed 模块（`xposedminversion=102`）

---

## 1. 它做什么

小米 HyperOS / MIUI 自带三套「让应用适应大屏」的机制，但只对系统内置名单里的应用开放：

| 机制 | 系统里的名字 | 效果 |
| --- | --- | --- |
| 平行窗口 | embedding | 左右两栏同时显示，一栏列表一栏详情 |
| 固定横屏 | fixedOrientation | 只支持竖屏的应用也能横过来（全屏拉伸 / 横屏信箱两档） |
| 通用全屏 | fullScreen | 铺满整屏，不分栏 |
| 界面自动适配 | autoui | 大屏上放大字体、拉宽布局（可与上面三者同时使用） |

本模块的作用是：**把你指定的应用「加进」这些名单，并把每条规则的全部参数开放给你手动调**。

### 适用环境

- 小米 / Redmi 的 HyperOS、MIUI（依赖 `MiuiEmbeddingWindow`、`autoui` 等系统私有实现）
- 需要 `persist.wm.extensions.enabled=true`
- 已实测机型：`25113PN0EC`（Android 17 / `V816` / `OS4.0`）
- 需要 Magisk + LSPosed（Zygisk 版本亦可）

> 非小米系统（原生 / 一加 / 三星等）不适用，模块会挂钩失败并在日志中报错。

---

## 2. 安装与启用

1. 安装 APK。
2. 打开 **LSPosed 管理器 → 模块 → 完美横屏**，打开模块开关。
3. 作用域**只勾「系统框架」（Android Framework）**。
   - 所有注入都发生在 `system_server` 进程里，不需要勾任何被适配的应用。
   - 「完美横屏」自身会被自动勾上，仅用来回显激活状态、接收页面抓取结果。
4. **重启手机**。
5. 重新打开模块界面，顶部应显示「模块已生效」。若显示「模块还没生效」，回到第 2、3 步检查。

> 所有配置改动（包括应用规则和进阶设置）都需要重启手机才会生效，界面底部会提示「改完需要重启手机才会生效」。

---

## 3. 主界面

| 区域 | 说明 |
| --- | --- |
| 顶部状态卡 | 「模块已生效 / 还没生效」+「已经设置了 N 个应用」 |
| 应用设置 → 选择应用 | 进入应用列表。**只有在这里设置过的应用才会被处理，没设置的完全不动**（模块没有总开关） |
| 在 LSPosed 里要勾哪些 | 作用域建议说明 |
| 进阶设置（不懂就别改） | 13 项底层注入开关，见第 8 节 |
| 右上角 ⋮ → 隐藏桌面图标 | 见第 7 节 |

---

## 4. 选择应用

进入「选择应用」后：

- **搜索框**：按应用名或包名过滤。
- **筛选标签**：
  - `全部`
  - `系统已内置` —— 系统规则文件里本来就有规则的应用
  - `已设置` —— 你在本模块里配置过的应用
  - `已安装` —— 用户安装的应用
  - `系统应用`
- **批量操作**：勾选多个应用后可「套用」同一模式或「清除设置」，也可「选中当前列表全部 / 取消选中」。

列表项上会显示「系统已内置」标记，可能是「平行窗口 / 固定横屏 / 固定横屏（被系统禁用）/ 界面适配」。

> 若提示「读不到系统内置规则文件」，说明机型的规则文件路径与预期不同，此时「系统已内置」标记不可用，但不影响正常配置。

### 系统内置值继承

点进一个从未配置过的应用时：

- 如果系统内置里有它的规则，详情页顶部会提示
  「这个应用系统本来就有内置规则（…），下面已经按系统的值填好，你可以直接在此基础上改」，
  各字段初值直接来自系统规则文件。
- 如果没有内置规则，则用模块默认值。

规则文件读取顺序：`/product/etc/` → `/system_ext/etc/` → `/system/etc/`（只读，模块不会修改它们）。

---

## 5. 应用详情页

### 5.1 顶部

- **对这个应用生效**：这条规则的开关。关掉后该应用完全不处理。
- **选择模式**（四选一）：

| 模式 | 含义 |
| --- | --- |
| 不处理 | 不为这个应用启用任何窗口机制 |
| 通用全屏 | 只走 fullScreen |
| 平行窗口 | 走 embedding |
| 固定横屏 | 走 fixedOrientation |

> **优先级：固定横屏 > 平行窗口 > 通用全屏。**
> 系统内部命中高优先级后就短路返回，低优先级的配置不会再被读取。所以三种模式只能选一个。

### 5.2 冲突提示

页面顶部有一张冲突卡片，会实时检查并高亮出问题的控件：

- **红色（严重）**：例如同时打开了多个系统开关、分屏比例填了非数字、默认档位不是 `full`/`fo`。有红色冲突时**无法保存**（提示「有严重冲突，请先修正再保存」）。
- **黄色（警告）**：例如当前是固定横屏模式却填了平行窗口参数（不会生效）、`skipSelfAdaptive` 被关掉、`supportModes` 改成了非 `full,fo` 的值。
- 手动指定系统开关且存在红色冲突时，会出现 **「按优先级自动修正」** 按钮，一键只保留优先级最高的那一个开关。

### 5.3 底部

- **保存** / **恢复默认**
- 右上角 ⋮ → **抓取页面类名**（见第 6 节）

### 5.4 平行窗口设置

留空表示「不设置这一项 / 跟随系统默认」。页面都填 **Activity 全类名**，`*` 代表全部，多个用英文逗号 `,` 隔开。

**开关标签**

| 界面名称 | 属性名 | 默认 | 说明 |
| --- | --- | --- | --- |
| 可放大到整屏 | `supportFullSize` | 开 | 允许从分栏切到整屏 |
| 显示中间分割线 | `isShowDivider` | 开 | 两栏之间画分割线 |
| 跳过应用自适应 | `skipSelfAdaptive` | 开 | **建议保持开启**，系统内置全部 embedding 规则都带此项，关掉后规则可能被自适应逻辑跳过 |
| 允许相机预览分屏 | `supportCameraPreview` | 关 | 相机预览页面也参与分栏 |
| 切换时重启应用 | `relaunch` | 关 | 切档时重建 Activity |
| 返回时清空上层页面 | `clearTop` | 关 | — |
| 左栏关闭时右栏一起关 | `finishSecondaryWithPrimary` | 关 | — |

**哪些页面参与**

| 界面名称 | 属性名 | 填写格式 |
| --- | --- | --- |
| 参与分屏的页面 | `activityRule` | Activity 全类名，多个用英文逗号隔开 |
| 左右两栏的配对方式 | `splitPairRule` | `左栏页面:右栏页面`，如 `MainActivity:*` |
| 右栏默认打开的页面 | `placeholder` | `主页面:占位页面` |
| 不做切换动画的页面 | `transitionRules` | 多个用英文逗号隔开 |
| 始终竖着显示的页面 | `forcePortraitActivity` | 这些页面不参与分屏 |

**分栏与外观**

| 界面名称 | 属性名 | 取值 |
| --- | --- | --- |
| 左栏宽度占比 | `splitRatio` | 0~1 的小数，如 `0.35` |
| 分栏最小宽度 | `splitMinWidth` | 低于该宽度不分栏，单位 dp |
| 整屏显示方式 | `fullRule` | 跟随系统默认 / `*` 所有页面都可整屏 / `nra` 整屏时不重建页面 / `nra:cr:rcr` 不重建 + 裁剪圆角 |
| 画面缩放方式 | `scaleMode` | 跟随系统默认 / `1` 等比缩放 |
| 居中显示范围 | `middleRule` | 跟随系统默认 / `*` 所有页面都居中 |
| 分割线颜色 | `splitLineColor` | `浅色:深色`，如 `#E6E6E6:#323232` |

**进阶参数**

| 界面名称 | 属性名 | 说明 |
| --- | --- | --- |
| 生效的最低应用版本 | `minSupportVersion` | `序号:日期`，如 `1:20230510` |
| 额外标记 | `flags` | 如 `forceResumeOnFocus:com.x.MainActivity` |
| 进程级兼容处理 | `procCompat` | 按应用进程名做兼容，一般留空 |
| 顺带启用的界面适配规则 | `autoUiRule` | `页面:数值`，多个用分号隔开 |
| 默认档位 | `defaultSettings` | 应用第一次打开时用哪一档 |

### 5.5 固定横屏设置

> 「全屏拉伸」会把画面铺满整个屏幕，可能变形；「横屏信箱」保持原比例居中显示，两边留黑边。

**开关标签**

| 界面名称 | 属性名 | 默认 | 说明 |
| --- | --- | --- | --- |
| 可放大到整屏 | `supportFullSize` | 开 | — |
| 按比例放大画面 | `isScale` | 关 | — |
| 切换时重启应用 | `relaunch` | 关 | — |
| 允许相机预览 | `supportCameraPreview` | 关 | — |
| 跳过兼容模式 | `skipCompatMode` | 关 | — |
| 竖屏也允许分栏 | `allowEmbInPortrait` | 关 | — |
| 换档时结束进程 | `forceKillWhenSwitch` | 关 | — |
| 无视系统禁用名单 | `disable` | **开** | 系统内置名单里有近 200 条固定横屏规则被标记为 `disable="true"`，开启后不理会这个标记 |

**下拉选项**

| 界面名称 | 属性名 | 可选值 |
| --- | --- | --- |
| 支持哪些档位 | `supportModes` | `full,fo`（全屏拉伸 + 横屏信箱，系统内置规则里固定是这个）/ 跟随系统默认 |
| 默认用哪一档 | `defaultSettings` | `fo` 横屏信箱（画面居中留黑边）/ `full` 全屏拉伸（铺满屏幕）/ 跟随系统默认 |
| 系统兼容性开关 | `compatChange` | 跟随系统默认 / 限制最小长宽比（中等，最常用）/ 限制最小长宽比（较大）/ 未声明方向时按竖屏处理 / 强制允许改变窗口大小 |

**输入项**

| 界面名称 | 属性名 | 说明 |
| --- | --- | --- |
| 始终竖着显示的页面 | `forcePortraitActivity` | 多个用英文逗号隔开 |
| 全屏档下仍竖着显示的页面 | `fullForcePortraitActivity` | 只在全屏拉伸档生效 |

### 5.6 界面自动适配（autoui）

让应用在大屏上放大字体、拉宽布局，**可以和上面的模式一起用**。

| 界面名称 | 属性名 | 说明 |
| --- | --- | --- |
| 开启界面自动适配 | `autoUiEnable`（默认关） | 这一段的总开关 |
| 顺带优化网页内容 | `optimizeWebView` | WebView 内容一起适配 |
| 需要适配的页面 | `activityRule` | `页面:数值`，多个用分号隔开 |
| 跳过适配的页面 | `skippedActivityRule` | 适配后显示异常的页面填这里 |
| 跳过的屏幕变化事件 | `skippedAppConfigChange` | 一般留空 |
| 本条规则版本号 | `versionCode` | 改了规则想让系统重新读取时才需要加 1 |

### 5.7 手动指定系统开关（高级）

打开「手动指定系统开关」后，就不再按上面选的模式自动决定系统开关，需要你自己保证三个开关不冲突。

| 界面名称 | 属性名 |
| --- | --- |
| 平行窗口开关 | `embeddedEnable` |
| 固定横屏开关 | `fixedOrientationEnable` |
| 全屏拉伸开关 | `fullScreenEnable` |

三个开关分别对应系统设置里的平行窗口、固定横屏、全屏拉伸，**一般只需要打开其中一个**。同时打开多个会报红，可用「按优先级自动修正」一键处理。

若不打开这一项，模块按所选模式自动映射：

```
不处理     → 三个开关全关
通用全屏   → 只开 全屏拉伸
平行窗口   → 只开 平行窗口
固定横屏   → 只开 固定横屏
```

---

## 6. 页面抓取工具

填 `activityRule` 之类的字段需要 Activity 全类名，手动查很麻烦，模块内置了抓取工具。

**使用步骤**

1. 进入某个应用的详情页 → 右上角 ⋮ → **抓取页面类名**。
2. 打开「开始记录打开过的页面」开关。
   - 刚打开时可能需要等几秒才开始生效。
   - 该开关等价于主界面进阶设置里的抓取功能，会在 `system_server` 侧记录 Activity 启动。
3. 回到桌面，打开目标应用，把要设置的页面都点一遍。
4. 回到抓取页面，列表会显示「共抓到 N 个页面」，勾选需要的条目。
5. 点 **填入所选字段**，在弹窗里选择要写进哪个设置项：
   - 参与分屏的页面（`activityRule`）
   - 不做切换动画的页面（`transitionRules`）
   - 始终竖着显示的页面（平行窗口 `forcePortraitActivity`）
   - 始终竖着显示的页面（固定横屏 `forcePortraitActivity`）
   - 全屏档下仍竖着显示的页面（`fullForcePortraitActivity`）
   - 需要适配的页面（界面适配 `activityRule`）
   - 跳过适配的页面（界面适配 `skippedActivityRule`）
6. 类名会以英文逗号并入原有内容并自动去重。回详情页 **保存**。
7. 抓完后可「清空记录」。

> **建议平时关闭抓取开关。** 开启后模块会在开机时挂钩 Activity 创建流程；关闭后模块在无配置时可做到完全不挂钩，开机更快。

---

## 7. 隐藏桌面图标

主界面右上角 ⋮ → **隐藏桌面图标**（可勾选项）。

- 勾选前会弹确认框：「隐藏后桌面和应用列表里不再显示本模块。之后请从 LSPosed 管理器 → 模块 → 本模块 → 模块设置 进来。」
- 隐藏后**功能完全不受影响**，仅桌面图标消失。
- **找回入口**：LSPosed 管理器 → 模块 → 完美横屏 → 模块设置。
- 再次进入后取消勾选该菜单项即可恢复桌面图标。

实现方式是停用一个只承载桌面图标的 `activity-alias`，主界面本身保留 LSPosed 的模块设置入口，因此不会出现「隐藏后再也进不去」的情况。

---

## 8. 进阶设置（不懂就别改）

主界面「进阶设置」一栏，全部为底层注入开关。除特别说明外，默认值都是经过真机验证的推荐值。

| 开关 | 默认 | 说明 |
| --- | --- | --- |
| 开机检查系统开关 | 开 | 开机时检查系统的平行窗口、大屏适配总开关是否打开，并记入日志 |
| 系统关了平行窗口时强行打开 | 开 | 系统总开关被关掉时，骗系统说它是开着的 |
| 系统关了大屏适配时强行打开 | 开 | 同上，针对 autoui |
| 替应用打开「设置里的开关」 | 开 | 自动在系统设置里给应用打开平行窗口／横屏开关，**效果最明显，建议保持开启** |
| 让系统认为应用在名单里 | 开 | 系统查「这个应用支持不支持」时直接回答支持 |
| 连「已开启」也一并谎报 | 开 | 系统多处会问「当前是否已开启」，一律回答已开启，兜底更彻底 |
| 直接写进系统规则表 | 开 | 把固定横屏规则塞进系统内部规则表并阻止系统清理；依赖机型内部结构，出问题可关掉 |
| 忽略系统的禁用名单 | 开 | 不再理会内置名单里近 200 条 `disable="true"` 标记 |
| 注入大屏布局规则 | 开 | 把界面适配规则写成一份「云端配置」交给系统读取 |
| 配置版本号 | `99999999` | **必须大于系统自带的 360816**，否则系统会继续用它自己的规则 |
| 让系统自己写版本号 | 关 | 更温和的抬高版本号方式，兼容性更好但属**实验功能** |
| 开始记录打开过的页面 | 关 | 页面抓取功能开关，用完建议关掉 |
| 输出详细日志 | 关 | 把每一次判断结果都写进 LSPosed 日志，排查问题时再开 |

---

## 9. 配置存放位置

| 用途 | 位置 |
| --- | --- |
| 模块自己的配置 | 模块私有 SharedPreferences `magic_window_config`（键 `global_config`、`app_rules`），通过 `xposedsharedprefs` 供 `system_server` 侧读取 |
| 系统内置规则（只读） | `/product/etc/` → `/system_ext/etc/` → `/system/etc/` 下的 `embedded_rules_list*.xml`、`fixed_orientation_list*.xml`、`autoui_list.xml` |
| 页面抓取记录 | 模块私有 SharedPreferences `magic_window_capture`（键 `records`，元素形如 `包名|类名`），由 `system_server` 经 ContentProvider 回传 |

模块**不会修改任何系统分区文件**，全部通过运行时挂钩实现。

---

## 10. 典型使用流程

**例：让某个只支持竖屏的应用横屏显示**

1. 选择应用 → 搜到目标应用 → 进入详情页。
2. 模式选 **固定横屏**。
3. 「默认用哪一档」选 `fo` 横屏信箱（保守，不变形）或 `full` 全屏拉伸（铺满，可能变形）。
4. 「系统兼容性开关」通常选「限制最小长宽比（中等，最常用）」。
5. 保存 → 重启手机。
6. 重启后打开该应用，横过屏幕验证。若某些页面（如登录页）必须竖屏，用抓取工具把它填到「始终竖着显示的页面」。

**例：让某个应用支持平行窗口**

1. 模式选 **平行窗口**，确认「跳过应用自适应」是开着的。
2. 用抓取工具抓出主页面和详情页，填进「参与分屏的页面」。
3. 「左右两栏的配对方式」填 `主页面类名:*`。
4. 需要时调「左栏宽度占比」（如 `0.35`）。
5. 保存 → 重启手机。

---

## 11. 排查与常见问题

**日志位置**

```
/data/adb/lspd/log/modules_*.log
```

用 root 查看，搜索模块 TAG 即可。需要更详细信息时打开「输出详细日志」（用完记得关，否则日志量很大）。

正常开机时模块日志应该只有十来行，注入全流程在 10 ms 以内完成。

**常见问题**

| 现象 | 处理 |
| --- | --- |
| 顶部显示「模块还没生效」 | LSPosed 里模块未启用，或作用域没勾「系统框架」；改完需重启 |
| 改了配置没变化 | 必须重启手机；确认该应用的「对这个应用生效」是打开的 |
| 保存不了 | 有红色严重冲突，先按提示修正或点「按优先级自动修正」 |
| 填了平行窗口参数但没效果 | 检查模式是不是选成了「固定横屏」（优先级更高会短路） |
| 界面适配（autoui）不生效 | 检查「配置版本号」是否大于 360816；检查「注入大屏布局规则」是否开启 |
| 提示读不到系统内置规则文件 | 机型规则文件名不同，仅影响「系统已内置」标记，不影响配置生效 |
| 应用横屏后画面变形 | 「默认用哪一档」从 `full` 改为 `fo` |
| 某些页面横屏后错乱 | 把这些页面填到「始终竖着显示的页面」/「跳过适配的页面」 |
| 抓取工具抓不到页面 | 确认记录开关已打开并等几秒；记录由系统侧批量回传，操作完稍等再回来看 |
| 桌面找不到图标 | 已开启「隐藏桌面图标」，从 LSPosed 管理器 → 模块 → 完美横屏 → 模块设置进入 |

**开机变慢？**

模块已针对开机性能做过优化：热路径只读内存快照、抓取回传异步批量、一次性挂钩用完即摘、无配置时根本不挂钩、热路径日志惰性求值。若仍觉得慢，可依次尝试：

1. 关闭「开始记录打开过的页面」；
2. 关闭「输出详细日志」；
3. 清理不再需要的应用规则（应用列表 → 批量「清除设置」）。

---

## 12. 风险提示

- 本模块挂钩的是系统私有实现，**换机型 / 大版本升级后可能失效**。
- 「直接写进系统规则表」「让系统自己写版本号」依赖内部结构，遇到异常（开机卡住、设置崩溃）优先关掉这两项。
- 如果改坏了导致无法开机：进 Recovery 或用 Magisk 的模块禁用方式停用 LSPosed（在 `/data/adb/modules/zygisk_lsposed/` 下创建 `disable` 文件），开机后再改回来。
